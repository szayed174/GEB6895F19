#####Write an R function that reads two numbers, multiplies them together and prints out their
product. You could start out with multiply two <- function(num 1, num 2) { ... }######

multiply_two <- function(num 1, num 2)
{

multiply <- (num_1*num_2)

return (multiply)
}

####End#####
